# python3 scripts/capture.py: check-citations, run against a throwaway git repository seeded from examples/check-citations
[GATE] Sources/Room.swift:2 (check-citations) `loadWidget` no longer exists: // Replaces `loadWidget`, which read the whole file into memory.
[GATE] docs/NOTES.md:3 (check-citations) `loadWidget` no longer exists: A `Room` reads its contents with `loadGadget`, or with `loadWidget` if
