# python3 scripts/capture.py: check-cli-flags, run against a throwaway git repository seeded from examples/check-cli-flags
[advisory] README.md:3 (check-cli-flags) `scripts/build.py --help` has no local grant in claims.local.toml — under `[check-cli-flags]`, add "scripts/build.py --help" to an existing `allowed` list or start one with `allowed = ["scripts/build.py --help"]`, or the same into `denied` to skip it and record that choice
[advisory] README.md:4 (check-cli-flags) `./manage.py --help` has no local grant in claims.local.toml — under `[check-cli-flags]`, add "./manage.py --help" to an existing `allowed` list or start one with `allowed = ["./manage.py --help"]`, or the same into `denied` to skip it and record that choice
