# Widget

Build it with `scripts/build.py --release`, and run it with
`manage.py`, which accepts `--verbose` for a running commentary. A
development dependency is added with `npm install --save-dev`.

```sh
scripts/build.py --jobs 4
```
