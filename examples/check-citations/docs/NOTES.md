# Notes

A `Room` reads its contents with `loadGadget`, or with `loadWidget` if
you need the whole file in memory at once.

<!-- was: loadWidget -->
`loadWidget` was retired because it read the whole file into memory.
